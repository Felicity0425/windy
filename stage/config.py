"""Central configuration for the wind reconstruction pipeline.

Stage scripts should import from `stage.pipeline_config` / `pipeline_config`
rather than depending on `hello.py`.
"""

from pipeline_config import *  # noqa: F401,F403
